name: First
on:
  push:
  pull_request:
    types: [closed]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Hello!
        run: |
          echo "Hello, world!"
