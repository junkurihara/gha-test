name: First
on:
  push:
    branches:
      - develop
  pull_request:
    types: [closed]
    branches:
      - main

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Hello!
        run: |
          echo "Hello, world!"
